---
import { site } from "../content/site";
---

<section id="servicios" class="bg-white py-16">
  <div class="mx-auto max-w-6xl px-4">
    <header class="max-w-2xl">
      <h2 class="text-3xl font-extrabold tracking-tight text-slate-900">Servicios</h2>
      <p class="mt-3 text-slate-600">
        Soluciones legales enfocadas en prevención, negociación y litigio cuando corresponde.
      </p>
    </header>

    <div class="mt-10 grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-4">
      {site.services.map((s) => (
        <article class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm hover:shadow-md transition-shadow">
          <h3 class="text-lg font-bold text-slate-900">{s.title}</h3>
          <p class="mt-2 text-sm leading-relaxed text-slate-600">{s.text}</p>
          <a href="#contacto" class="mt-4 inline-flex text-sm font-semibold text-rose-950 hover:underline">
            Consultar →
          </a>
        </article>
      ))}
    </div>
  </div>
</section>
