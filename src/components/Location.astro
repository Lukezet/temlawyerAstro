---
import { site } from "../content/site";

const fullAddress = `${site.address.street}, ${site.address.city}, ${site.address.region}`;
const hasEmbed = Boolean((site as any).googleMapsEmbedUrl);
const embedUrl = (site as any).googleMapsEmbedUrl as string | undefined;
---

<section id="ubicacion" class="bg-white py-16">
  <div class="mx-auto max-w-6xl px-4">
    <header class="max-w-2xl">
      <p class="text-xs font-semibold tracking-[0.2em] text-amber-700 uppercase">Ubicación</p>
      <h2 class="mt-3 text-3xl font-extrabold tracking-tight text-slate-900">
        Encontranos en {site.address.city}
      </h2>
      <p class="mt-3 text-slate-600">
        Atención presencial con cita previa y modalidad online según el caso.
      </p>
    </header>

    <div class="mt-10 grid grid-cols-1 gap-6 lg:grid-cols-2 lg:items-stretch">
      <!-- Card info -->
      <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
        <dl class="space-y-4 text-sm">
          <div>
            <dt class="font-semibold text-slate-900">Dirección</dt>
            <dd class="mt-1 text-slate-600">{fullAddress}</dd>
          </div>

          <div class="grid grid-cols-1 gap-4 sm:grid-cols-2">
            <div>
              <dt class="font-semibold text-slate-900">Teléfono</dt>
              <dd class="mt-1">
                <a class="font-semibold text-emerald-950 hover:underline" href={`tel:${site.phone}`}>
                  {site.phone}
                </a>
              </dd>
            </div>

            <div>
              <dt class="font-semibold text-slate-900">Email</dt>
              <dd class="mt-1">
                <a class="font-semibold text-emerald-950 hover:underline" href={`mailto:${site.email}`}>
                  {site.email}
                </a>
              </dd>
            </div>
          </div>

          {site.hours && site.hours.length > 0 && (
            <div>
              <dt class="font-semibold text-slate-900">Horarios</dt>
              <dd class="mt-2 space-y-2 text-slate-600">
                {site.hours.map((h) => (
                  <div class="flex items-center justify-between gap-4 rounded-2xl bg-slate-50 px-4 py-2">
                    <span class="font-semibold text-slate-700">{h.label}</span>
                    <span class="text-slate-600">{h.value}</span>
                  </div>
                ))}
              </dd>
            </div>
          )}

          <div class="pt-2 flex flex-wrap gap-3">
            <a
              href={site.googleMapsUrl}
              target="_blank"
              rel="noopener noreferrer"
              class="inline-flex items-center justify-center rounded-full bg-emerald-950 px-5 py-3 text-sm font-semibold text-white hover:bg-emerald-900 focus:outline-none focus:ring-2 focus:ring-emerald-950/30"
            >
              Cómo llegar
            </a>

            <a
              href="#contacto"
              class="inline-flex items-center justify-center rounded-full border border-slate-200 px-5 py-3 text-sm font-semibold text-slate-900 hover:bg-slate-50"
            >
              Coordinar cita
            </a>
          </div>

          <p class="mt-4 text-xs text-slate-500">
            Recomendación: consultá disponibilidad antes de acercarte para asegurar una atención ágil.
          </p>
        </dl>
      </div>

      <!-- Map -->
      <div class="overflow-hidden rounded-3xl border border-slate-200 bg-slate-50 shadow-sm">
  <iframe
    class="h-[420px] w-full"
    src={site.googleMapsEmbedUrl}
    loading="lazy"
    referrerpolicy="no-referrer-when-downgrade"
    aria-label="Mapa de ubicación del estudio jurídico"
  />
</div>

    </div>
  </div>
</section>
